#include "main.h"

/**
 * mul - multiply two given numbers and return the answer
 *
 * @a: holds the first number
 * @b: holds the second number
 *
 * Return: returns the value of the multiplication process
 */

int mul(int a, int b)
{
	int ret;

	ret = a * b;
	return (ret);
}
