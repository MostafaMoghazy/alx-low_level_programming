#include "main.h"

/**
 * add - adds two integers and return the result
 *
 * @f: first input
 * @s: second input
 *
 * Return: the sum of the two inputs
 */

int add(int f, int s)
{
	int sum = f + s;

	return (sum);
}
